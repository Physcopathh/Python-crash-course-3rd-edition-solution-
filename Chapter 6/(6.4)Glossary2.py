glossary = {
    'if':'this is conditional and check if the statement is okay?',
    'elif':'this check when if passes and then this is another if statement after the actaul if',
    'else':'when nothing works then this works',
    'what if':'this don\'t exist bro',
    'bro':'codes!!!',
    'keys':'for checking keys',
    'values':'the value by key',
    'key':'direction for key',
    'hello':'world',
    'og':'not python but django'
}
for g,h in glossary.items():
    print(g,':',h,'\n')